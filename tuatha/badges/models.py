"""tuatha.badges.models — the Pydantic v2 models for the badges (Student + Badge + QuestPack + AchievementToken).

Per the educational-credential badge system (replaces the
legacy Crypteolas financial token from the prior pivot).
"""
from __future__ import annotations

from typing import Any


class ModelsStub:
    """The models stub class."""

    def __init__(self, *args: Any, **kwargs: Any) -> None:
        pass


__all__ = ["ModelsStub"]
