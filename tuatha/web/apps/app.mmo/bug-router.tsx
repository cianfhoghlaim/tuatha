// tuatha.web.apps.app.mmo.bug-router — a placeholder router.
// In production: replaced by `routeTree.gen.ts` from TanStack Start's CLI.
// This stub exists so the entry point compiles standalone.
import { ReactNode } from "react";

export function BugRouter({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
