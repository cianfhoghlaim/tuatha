"""tuatha/sources/official — rung-1 (Document) + rung-2 (Location) layer.

The 8 British Isles jurisdictions + 5 safeguarding bodies → DuckDB rows
with sha256_hash, page_count, source_kind, fetched_at.

The real implementation lives in cianfhoghlaim/dlt_sources/cultural_heritage/
(per the wholesale-copy convention). The previous session's
'40 inert DLT sources' will be REPLACED by a real call into this layer
in Phase 1 (W1).
"""
