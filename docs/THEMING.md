# tuatha Theming

The British Isles Formative Assessment MMO theme per the
canonical `cianfhoghlaim-educational-mmo` spec.

## The KEEPS (the technological choices)

- The 8 NCCA subject agents + the 5 per-subject tools (40 tools)
- The 12-agent fleet pattern (root_agent + curriculum_agent + ...)
- The 3 educational agents (academic_history + celtic_grammar
  + celtic_morphology)
- The 4 BIEP hackathon features (marking_grader + adaptive_tutor
  + equivalency_generator + curriculum_change_sensor)
- The 1 media_intel pipeline (the 10-tool `media_descriptor_agent`)
- The BAML + DLT + Dagster + CocoIndex + marimo pipeline stack
- The Hono + Convex + TanStack Start + CopilotKit web stack
- The LiteLLM + Cognee + Graphiti + LanceDB + Letta memory stack
- The educational-credential badge system (replaces the
  legacy Crypteolas financial token)

## The DROPS (the legacy theming — hard-archived)

- ~~Pent-Elemental Cosmology~~ (5 realms: Spirit / Water /
  Fire / Earth / Air) — replaced by the British Isles
  Formative Assessment MMO theme
- ~~Babylon.js 3D~~ game front-end — replaced with the
  TanStack Start 2D client
- ~~SpacetimeDB v2~~ game engine backend — replaced with
  Convex + Hono + Dagster + DuckLake
- ~~Crypteolas financial token~~ — replaced with the
  educational-credential badge system
- ~~Anam Cara soul friend mechanic~~ — replaced with the
  4 BIEP hackathon features
- ~~Brown Ajah theming~~ — replaced with the canonical
  8 NCCA subject agent fleet

## The Celtic MMO design — NOT in this change

The Celtic MMO design (which elements, what boons, the
4+1 element binding, the sub-nation mapping, the 2D particle
renderer choice, the iOS delivery vehicle) is the downstream
theming change gated on the corpus being populated. **NOT
in this change.**
